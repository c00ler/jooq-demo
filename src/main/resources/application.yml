spring:
  main:
    banner-mode: off
  application:
    name: jooq-demo
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${db.url}
    username: ${db.username}
    password: ${db.password}
  jooq:
    sql-dialect: postgres
